export enum ZbStatus {
  Valid = "valid",
  Invalid = "invalid",
  DoNotMail = "do_not_mail",
}

export enum ZbSubStatus {
  PossibleTypo = "possible_typo",
  RoleBased = "role_based",
  Disposable = "disposable",
}
